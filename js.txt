//OMDb API: http://www.omdbapi.com/?i=tt3896198&apikey=ab3f337e
//https://wiki.openstreetmap.org/wiki/Tag:amenity%3Dcinema      //// this is the page that refers to the movie theatre

// Develop a movie and TV show database where users can search for films, 
// view details, and access ratings and reviews. Utilize the OMDB API to fetch movie and TV show data, including titles, 
// cast, plot summaries, and posters. We can intergrate OpenStreetMap Api to make get the users location and find the newarest movie theatre.


var apiKey=  "ab3f337e";

 
    // Initialize the map
    var map = L.map('map').setView([40.7128, -74.0060], 12);

    // Create a tile layer using OpenStreetMap data
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: 'Map data © <a href="https://openstreetmap.org">OpenStreetMap</a> contributors'
    }).addTo(map);

    // Add a marker at a specific location
    var marker = L.marker([40.7128, -74.0060]).addTo(map);
    marker.bindPopup('New York City').openPopup();
//this part is css
    #map {
  height: 400px;
}